import { Link } from "react-router-dom";

function Header() {
    return (
        <header>
            <Link to="/">Pizza Company</Link>

            <p>Onur</p>
        </header>
    );
}

export default Header;
